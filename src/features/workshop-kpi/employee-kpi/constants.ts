import { BSC_COLORS, BSC_ENUM, BSC_ORDER } from '../../../constants/bsc';
import { MONTH_NAMES_SHORT, PERIOD_ENUM, PERIOD_LABELS, QUARTER_NAMES } from '../../../constants/period';
import { KPI_COLORS } from '../../../constants/uiTokens';

export const EMPLOYEE_KPI_COLORS = KPI_COLORS;
export const EMPLOYEE_KPI_BSC_ORDER = BSC_ORDER;
export const EMPLOYEE_KPI_BSC_COLORS = BSC_COLORS;

export const EMPLOYEE_KPI_PERIOD_ENUM = PERIOD_ENUM;
export const EMPLOYEE_KPI_PERIOD_LABELS = PERIOD_LABELS;

export const EMPLOYEE_KPI_QUARTERS = QUARTER_NAMES;
export const EMPLOYEE_KPI_MONTHS = MONTH_NAMES_SHORT;

export const EMPLOYEE_KPI_OTHER_BSC = BSC_ENUM.OTHER;
